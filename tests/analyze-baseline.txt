# gcc 13 widening artifact in hash_add (rtsp/hash.h): the trace records the
# allocation as succeeding yet follows ASSERT's failure branch — an infeasible
# path; restructuring the call site does not clear it.
rtsp/common.h: leak of 'p' [CWE-401] [-Wanalyzer-malloc-leak]
